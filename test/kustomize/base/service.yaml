apiVersion: v1
kind: Service
metadata:
  name: foo
  namespace: bar
  labels:
    app: bar
    component: foo
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: bar
    component: foo
